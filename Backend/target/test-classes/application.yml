server:
  port: 0

spring:
  application:
    name: audio-deepcheck-backend-test
  data:
    mongodb:
      database: audio_deepcheck_test
      auto-index-creation: true

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.2

ai:
  service:
    base-url: http://localhost:8000
    connect-timeout-ms: 2000
    read-timeout-ms: 5000
    analyze-timeout-ms: 5000
